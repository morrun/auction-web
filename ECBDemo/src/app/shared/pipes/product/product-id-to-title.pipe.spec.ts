import { ProductIdToTitlePipe } from './product-id-to-title.pipe';

describe('ProductIdToTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductIdToTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
