import { ProductViewPipe } from './product-view.pipe';

describe('ProductViewPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductViewPipe();
    expect(pipe).toBeTruthy();
  });
});
