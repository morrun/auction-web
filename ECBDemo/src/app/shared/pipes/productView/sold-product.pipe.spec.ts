import { SoldProductPipe } from './sold-product.pipe';

describe('SoldProductPipe', () => {
  it('create an instance', () => {
    const pipe = new SoldProductPipe();
    expect(pipe).toBeTruthy();
  });
});
