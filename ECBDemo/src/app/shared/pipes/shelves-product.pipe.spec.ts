import { ShelvesProductPipe } from './shelves-product.pipe';

describe('ShelvesProductPipe', () => {
  it('create an instance', () => {
    const pipe = new ShelvesProductPipe();
    expect(pipe).toBeTruthy();
  });
});
