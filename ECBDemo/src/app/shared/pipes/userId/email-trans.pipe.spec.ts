import { EmailTransPipe } from './email-trans.pipe';

describe('EmailTransPipe', () => {
  it('create an instance', () => {
    const pipe = new EmailTransPipe();
    expect(pipe).toBeTruthy();
  });
});
